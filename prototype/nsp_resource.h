// nsp_resource.h — resource ids, shared by silkplay.rc and the code.
#pragma once

// The lowest icon id is the one Explorer shows for the .exe, so the pink ribbon is 1.
#define IDI_SILK 1
// The same ribbon in grey: the tray icon while frame generation is switched off.
#define IDI_SILK_OFF 2
